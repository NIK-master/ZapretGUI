<UserControl x:Class="ZapretGUI.Views.WizardPages.Step5_FinishPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock x:Name="TxtStatus" Text="Применение настроек..." FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <ProgressBar x:Name="ProgBar" Width="350" Height="4" IsIndeterminate="True" Foreground="#107C10" Background="#181818" BorderThickness="0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#050505" CornerRadius="6" BorderBrush="#1A1A1A" BorderThickness="1" Padding="10">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Hidden">
                <TextBlock x:Name="ConsoleLog" FontFamily="Consolas" FontSize="12" Foreground="#4CAF50" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>