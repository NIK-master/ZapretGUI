<Application x:Class="ZapretGUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:localCore="clr-namespace:ZapretGUI.Core"
             Startup="Application_Startup">
    <Application.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <localCore:ActiveStatusToBrushConverter x:Key="ActiveStatusToBrushConverter" />
            <localCore:ActiveStatusToIconConverter x:Key="ActiveStatusToIconConverter" />
            <localCore:PingToBrushConverter x:Key="PingToBrushConverter" />

            <SolidColorBrush x:Key="BrandSuccessBrush" Color="#107C10" />
            <SolidColorBrush x:Key="BrandErrorBrush" Color="#D13438" />
            <SolidColorBrush x:Key="BrandWarningBrush" Color="#FF8C00" />

            <SolidColorBrush x:Key="SurfaceBackgroundBrush" Color="#151515" />
            <SolidColorBrush x:Key="CardBorderBrush" Color="#2A2A2A" />
            <SolidColorBrush x:Key="CardHoverBorderBrush" Color="#4A4A4A" />

            <Style x:Key="CyberToggleStyle" TargetType="CheckBox">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="#2A2A2A"/>
                <Setter Property="BorderBrush" Value="#3A3A3A"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Border x:Name="BgBorder" Width="44" Height="24" CornerRadius="12" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                    HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalAlignment}">
                                <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="#888888" HorizontalAlignment="Left" Margin="2,0,0,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform" X="0"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <!-- Динамически берем наш зеленый (или синий) цвет -->
                                    <Setter TargetName="BgBorder" Property="Background" Value="{DynamicResource BrandSuccessBrush}"/>
                                    <Setter TargetName="BgBorder" Property="BorderBrush" Value="{DynamicResource BrandSuccessBrush}"/>
                                    <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <!-- Плавный выезд кружка -->
                                                <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="20" Duration="0:0:0.25">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.25">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <!-- Фикс черного ховера -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Thumb" Property="Fill" Value="#E0E0E0"/>
                                    <Setter TargetName="BgBorder" Property="BorderBrush" Value="#555555"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                        <Condition Property="IsChecked" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="BgBorder" Property="Opacity" Value="0.85"/>
                                    <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>